<Window x:Class="AnimeRandomizer.Winners"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnimeRandomizer"
        mc:Ignorable="d"
        Title="Winners" Height="500" Width="370" MinWidth="250" MinHeight="160" Background="{x:Null}" Foreground="{x:Null}">
    <ListBox x:Name="ResultAnimeList" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Top" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="#00000000" BorderBrush="{x:Null}" BorderThickness="0"/>

</Window>
